version: '2'

services:
  web:
    restart: on-failure
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - /code/node_modules
